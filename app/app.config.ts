export default defineAppConfig({
  ui: {
    // Ramps are redefined in `assets/css/main.css` from the new brand system.
    colors: {
      primary: 'lime',
      secondary: 'slate',
      neutral: 'slate'
    }
  }
})
